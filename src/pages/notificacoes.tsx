import Layout from "@components/template/Layout"

export default function Notificacoes() {
    return (
        <Layout
            pagina={`Notificações`}
            titulo={`Notificações do Sistema`}
            subtitulo={`Personalize as notificações aqui.`}
        >
            <span>Notificações</span>
        </Layout>
    )
}